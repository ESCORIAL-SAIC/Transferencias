<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Transferencias.Views.Produccion.ProduccionAutorizarTransferenciaView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:cv="clr-namespace:Camera.MAUI;assembly=Camera.MAUI"
    xmlns:controls="clr-namespace:Transferencias.Controls"
    Shell.NavBarIsVisible="False">
    <Grid Padding="0,0" RowDefinitions="Auto,8*,*">
        <controls:TopBar
            Grid.Row="0"
            UseLogoutButton="True"
            ShowUserData="True" />
        <Border
            Grid.Row="1"
            Margin="5"
            Stroke="Transparent"
            StrokeShape="RoundRectangle 10"
            StrokeThickness="0">
            <cv:CameraView
                x:Name="CameraView"
                BarCodeDetectionEnabled="True"
                BarcodeDetected="CameraView_BarcodeDetected"
                CamerasLoaded="CameraView_CamerasLoaded"
                HeightRequest="300"
                WidthRequest="300" />
        </Border>
        <Label
            x:Name="BarcodeResult"
            Grid.Row="2"
            FontSize="20" />
    </Grid>
</ContentPage>
